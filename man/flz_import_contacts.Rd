% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lists.R
\name{flz_import_contacts}
\alias{flz_import_contacts}
\title{Importar contactos a una lista}
\usage{
flz_import_contacts(
  token,
  client_slug,
  list_id,
  file,
  fields,
  sync = NULL,
  ignorefirstline = NULL,
  autouniquecode = NULL,
  format = "json",
  base_url = "https://api.fidelizador.com"
)
}
\arguments{
\item{token}{Token de autenticación obtenido con \code{flz_auth()}}

\item{client_slug}{Slug del cliente en Fidelizador}

\item{list_id}{ID de la lista donde se importarán los contactos}

\item{file}{Ruta al archivo a importar}

\item{fields}{Lista nombrada donde los nombres son los tags de campos (en MAYÚSCULAS)
y los valores son los números de columna. Ejemplo: \code{list(EMAIL = 1, FIRSTNAME = 2)}}

\item{sync}{Indica si se debe sincronizar la lista. Valores: 0 o 1 (por defecto NULL)}

\item{ignorefirstline}{Indica si se debe omitir la primera línea (cabecera).
Valores: 0 o 1 (por defecto NULL)}

\item{autouniquecode}{Indica si debe generar automáticamente el Código Identificador.
Valores: 0 o 1 (por defecto NULL)}

\item{format}{Formato de respuesta: "json" o "xml" (por defecto "json")}

\item{base_url}{URL base de la API (por defecto: "https://api.fidelizador.com")}
}
\value{
Objeto con la respuesta de la API
}
\description{
Crea una nueva importación de contactos desde un archivo a una lista existente en Fidelizador.
}
\examples{
\dontrun{
token <- flz_auth()

# Importar contactos con campos básicos
resultado <- flz_import_contacts(
  token = token,
  client_slug = "mi-empresa",
  list_id = 123,
  file = "contactos.csv",
  fields = list(EMAIL = 1, FIRSTNAME = 2, LASTNAME = 3)
)

# Importar con opciones adicionales
resultado <- flz_import_contacts(
  token = token,
  client_slug = "mi-empresa",
  list_id = 123,
  file = "contactos.csv",
  fields = list(EMAIL = 1, FIRSTNAME = 2),
  sync = 1,
  ignorefirstline = 1,
  autouniquecode = 1
)
}
}
