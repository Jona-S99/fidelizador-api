% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/campaing.R
\name{flz_send_test_campaign}
\alias{flz_send_test_campaign}
\title{Enviar campaña de prueba}
\usage{
flz_send_test_campaign(
  token,
  client_slug,
  campaign_id,
  test_emails = NULL,
  test_list_id = NULL,
  test_contact = NULL,
  subject_prefix = NULL,
  format = "json",
  base_url = "https://api.fidelizador.com"
)
}
\arguments{
\item{token}{Token de autenticación obtenido con \code{flz_auth()}}

\item{client_slug}{Slug del cliente en Fidelizador}

\item{campaign_id}{ID de la campaña existente en estado borrador}

\item{test_emails}{Vector de correos electrónicos para enviar la prueba}

\item{test_list_id}{Opcional - ID de la lista de pruebas a utilizar (tiene prioridad sobre test_emails)}

\item{test_contact}{Opcional - Correo del contacto del cual se obtendrá la información para las pruebas}

\item{subject_prefix}{Opcional - Prefijo del asunto a utilizar}

\item{format}{Formato de respuesta: "json" o "xml" (por defecto: "json")}

\item{base_url}{URL base de la API (por defecto: "https://api.fidelizador.com")}
}
\value{
Objeto con la respuesta de la API
}
\description{
Envía una campaña de prueba a correos específicos para verificar que cumple
los requerimientos antes del envío definitivo.
}
\examples{
\dontrun{
token <- flz_auth()

# Enviar prueba a correos específicos
resultado <- flz_send_test_campaign(
  token = token,
  client_slug = "mi-empresa",
  campaign_id = 123,
  test_emails = c("test1@example.com", "test2@example.com")
)

# Con lista de prueba
resultado <- flz_send_test_campaign(
  token = token,
  client_slug = "mi-empresa",
  campaign_id = 123,
  test_list_id = 10
)

# Con prefijo en el asunto
resultado <- flz_send_test_campaign(
  token = token,
  client_slug = "mi-empresa",
  campaign_id = 123,
  test_emails = "test@example.com",
  subject_prefix = "[PRUEBA]"
)
}
}
