% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/category.R
\name{flz_get_categories}
\alias{flz_get_categories}
\title{Obtener listado de categorías}
\usage{
flz_get_categories(
  token,
  client_slug,
  name = NULL,
  page = NULL,
  items = NULL,
  format = "json",
  base_url = "https://api.fidelizador.com"
)
}
\arguments{
\item{token}{Token de autenticación obtenido con \code{flz_auth()}}

\item{client_slug}{Slug del cliente en Fidelizador}

\item{name}{Opcional - Filtrar categorías por nombre}

\item{page}{Opcional - Número de página (si hay paginación)}

\item{items}{Opcional - Cantidad de resultados por página (1-100)}

\item{format}{Formato de respuesta: "json" o "xml" (por defecto: "json")}

\item{base_url}{URL base de la API (por defecto: "https://api.fidelizador.com")}
}
\value{
Objeto con la respuesta de la API conteniendo las categorías
}
\description{
Obtiene un listado de todas las categorías en Fidelizador.
}
\examples{
\dontrun{
token <- flz_auth()

# Obtener todas las categorías
categorias <- flz_get_categories(
  token = token,
  client_slug = "mi-empresa"
)

# Filtrar por nombre
categorias <- flz_get_categories(
  token = token,
  client_slug = "mi-empresa",
  name = "Newsletter"
)

# Con paginación
categorias <- flz_get_categories(
  token = token,
  client_slug = "mi-empresa",
  page = 1,
  items = 50
)
}
}
